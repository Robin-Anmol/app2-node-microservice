name: payment-tests

on:
  pull_request:
    paths:
      - "payment/**"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: cd payment && npm install && npm run test:ci
